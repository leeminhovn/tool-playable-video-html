# Infrastructure

This directory contains external services implementations and configurations.

## Structure
- `/api` - API clients and configurations
- `/services` - External services implementations
- `/storage` - Storage implementations (localStorage, sessionStorage)
- `/config` - Application configurations