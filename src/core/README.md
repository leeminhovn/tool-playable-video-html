# Core

This directory contains the core business logic of the application.

## Structure
- `/entities` - Enterprise-wide business rules and entities
- `/usecases` - Application-specific business rules
- `/interfaces` - Abstract interfaces used by the business logic